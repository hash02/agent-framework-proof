apiVersion: v1
kind: Service
metadata:
  name: agent-framework-proof
  labels:
    app: agent-framework-proof
spec:
  type: ClusterIP
  selector:
    app: agent-framework-proof
  ports:
    - name: http
      port: 8000
      targetPort: http
